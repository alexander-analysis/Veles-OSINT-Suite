# Optional hardware integrations - install only on a Pi with an RTL-SDR dongle:
#   sudo apt-get install -y rtl-sdr librtlsdr-dev
#   venv/bin/pip install -r requirements-optional.txt
pyrtlsdr>=0.3
Pillow>=10.4
