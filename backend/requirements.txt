# VELES backend dependencies (Python >= 3.11; verified on 3.14).
#
# Lower bounds rather than exact pins: the Pi ships whichever Python its OS
# release carries (3.11 on Bookworm, 3.13 on Trixie) and the pinned versions
# in the original brief (late-2023) have no wheels for modern interpreters.

# Core framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Database & migrations
sqlalchemy>=2.0.36
alembic>=1.13

# Async HTTP
httpx>=0.27
aiofiles>=24.1

# Market data & analysis
ccxt>=4.4
yfinance>=0.2.50
pandas>=2.2
numpy>=2.0
scipy>=1.14

# Maritime data & geospatial
geopy>=2.4
pyproj>=3.7
shapely>=2.0
pyais>=2.6  # NMEA/AIS decoding (RTL-SDR, AIS-catcher, NMEA feeds)
websockets>=13

# Task scheduling (4.x is a rewrite with a different API - stay on 3.x)
APScheduler>=3.10,<4
pytz>=2024.1

# Data parsing & validation
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
PyYAML>=6.0.2

# Utilities
python-dateutil>=2.9
requests>=2.32
click>=8.1
colorama>=0.4.6
loguru>=0.7.2

# Development / testing
pytest>=8.3
pytest-asyncio>=0.24
black>=24.8
flake8>=7.1
