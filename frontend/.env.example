# Base URL for the API. Leave empty to use relative /api URLs (Vite proxy in
# dev, Nginx in production).
VITE_API_BASE=
# Classification banner shown at the top of every page
VITE_CLASSIFICATION=UNCLASSIFIED
